# zaomeng runtime dependencies for Termux

# Keep this set free of mandatory native Rust extensions. PyPI does not publish
# Android wheels for pydantic-core, so Pydantic 2 would require a local Rust build.

# Core
PyYAML>=6.0
requests>=2.31.0,<3.0.0

# Text processing
jieba>=0.42.1

# Web UI runtime
# Pin to the tested FastAPI line that accepts standalone Pydantic v1.
fastapi>=0.104.0,<0.120.0
# Pydantic 1.10.20+ supports Python 3.13 and publishes a pure-Python wheel.
pydantic>=1.10.20,<2.0.0
python-multipart==0.0.20
uvicorn>=0.24.0
