# 安全政策

造梦会在用户设备上处理模型 API 密钥、小说原文、人物资料和对话历史。任何可能泄露这些数据的问题都应按安全漏洞处理。

## 支持范围

安全修复优先应用于：

- `main` 分支；
- 最新公开版本；
- 当前维护的 KMP 应用与内嵌服务端。

旧 Python/Web 实现不再主动维护，但若漏洞影响当前导入格式、共享包或升级路径，也请一并报告。

## 私下报告

请通过 GitHub 仓库的 **Security** 页面使用私密漏洞报告功能。如果该入口不可用，请先通过维护者的 GitHub 主页建立私密联系方式。

不要在公开 Issue、Discussion、PR、日志或示例包中包含：

- 可用的 API key、访问令牌或 Authorization header；
- Android 签名文件及密码；
- 用户数据库、完整对话、人物隐私或受版权保护的完整原文；
- 可直接利用且尚未修复的攻击步骤。

报告建议包含：受影响版本/提交、平台、复现前提、最小复现、影响范围和建议修复。敏感附件应先确认安全传输方式。

## 重点安全边界

- 模型密钥必须保存在平台安全存储中，不得进入 Room 文档、manifest、日志或导出包。
- run/session ID、ZIP 条目和插件路径必须验证，防止路径穿越、覆盖和 ZIP bomb。
- 导入包和插件是不可信输入；需要限制文件数量、解压大小、压缩比和目标路径。
- 模型输出是不可信输入；结构化内容必须解析、校验 speaker/字段和权限边界后才能提交。
- 本地 SSE/HTTP 接口不应意外暴露到外部网络，也不能在错误响应中回显秘密。
- 日志、诊断和崩溃报告必须截断模型输出，不记录完整提示词、原文或密钥。
- 删除会话、书卷或密钥时，应检查 Room 领域表、文档表、缓存和安全存储的级联行为。

## 仓库秘密

以下内容不得提交：

- `*.jks`、`*.keystore`；
- `keystore.properties`、`local.properties`；
- `.env*`、真实 `config.yaml`；
- SQLite/Room 数据库、应用私有目录和用户导出；
- Gradle、IDE、Xcode 缓存和构建产物。

如果秘密已经进入 Git 历史，不要只删除当前文件：立即撤销/轮换秘密，并私下协调清理历史和发布安全修复。

## 修复披露

维护者确认问题后会评估影响、准备修复和回归测试，并在用户有合理升级窗口后协调披露。请在修复公开前保持报告内容私密。
